# VidMuse OSS 私有配置附录

此文件记录当前本机 `.env` 中的 OSS 连接配置，供迁移到另一个项目时人工参考。

注意：

- 此文件包含真实 OSS 访问凭据，不要提交到 git。
- 当前仓库 `.gitignore` 已忽略 `.env.*`，本文件默认不会被提交。
- 给另一个项目使用时，优先复制为目标项目的 `.env` 或部署环境变量，不要复制到公开文档。

## 当前真实配置

```env
STORAGE_ENDPOINT=https://oss-cn-beijing.aliyuncs.com
STORAGE_PUBLIC_BASE_URL=https://muse.oss-cn-beijing.aliyuncs.com
STORAGE_ACCESS_KEY=LTAI5t7fhosvHJeu3212psho
STORAGE_SECRET_KEY=jfD1ZOfvVMarI2Ew7SZ506YTkgG4VZ
STORAGE_BUCKET=guangxiprod
STORAGE_REGION=cn-beijing
STORAGE_SECURE=true
```

## 字段说明

| 变量 | 当前值说明 |
| --- | --- |
| `STORAGE_ENDPOINT` | OSS SDK 连接 endpoint，当前为北京 region |
| `STORAGE_PUBLIC_BASE_URL` | 对象基础访问地址，当前使用 `muse.oss-cn-beijing.aliyuncs.com` |
| `STORAGE_ACCESS_KEY` | 当前 OSS AccessKey ID |
| `STORAGE_SECRET_KEY` | 当前 OSS AccessKey Secret |
| `STORAGE_BUCKET` | 当前 bucket：`guangxiprod` |
| `STORAGE_REGION` | 当前 region：`cn-beijing` |
| `STORAGE_SECURE` | 使用 HTTPS |

## 给验证脚本的变量映射

`scripts/test_oss_private_bucket.py` 使用 `OSS_*` 变量名，运行前可这样映射：

```powershell
$env:SystemRoot='C:\Windows'
$env:windir='C:\Windows'
$env:COMSPEC='C:\Windows\System32\cmd.exe'

$env:OSS_ACCESS_KEY_ID=$env:STORAGE_ACCESS_KEY
$env:OSS_ACCESS_KEY_SECRET=$env:STORAGE_SECRET_KEY
$env:OSS_BUCKET=$env:STORAGE_BUCKET
$env:OSS_ENDPOINT='oss-cn-beijing.aliyuncs.com'
$env:OSS_REGION=$env:STORAGE_REGION
$env:OSS_PUBLIC_BASE_URL=$env:STORAGE_PUBLIC_BASE_URL
python scripts/test_oss_private_bucket.py
```
